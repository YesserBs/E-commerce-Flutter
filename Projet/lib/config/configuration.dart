import 'package:flutter/material.dart';

class config {
  // colors
  static const Color primaryColor = Color(0xFF0093FF);
  static const Color backgroundColor = Color(0xFFDCF0FF);
  static const Color secondaryColor = Color(0xFF004A82);

  // font sizes
  static const double FontSizeBigTitle = 135;
  static const double FontSizeTitle = 90;
  static const double FontSizeNormal = 46;
  static const double FontSizeSmall = 39;

  // font families
  static const String font = 'OstrichSans';
}
