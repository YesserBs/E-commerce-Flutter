import 'package:flutter/material.dart';

class config {
  // colors
  static const Color primaryColor = Color(0xFF0093FF);
  static const Color backgroundColor = Color(0xFFDCF0FF);
  static const Color secondaryColor = Color(0xFF004A82);

  // font sizes
  static const double FontSizeBigTitle = 45;
  static const double FontSizeTitle = 30;
  static const double FontSizeNormal = 18;
  static const double FontSizeSmall = 13;

  // font families
  static const String font = 'OstrichSans';
}
